pipeline {
    agent {
        docker { 
			image 'jenkins-windows:2.0'
		}
    }
    stages {
        stage('Test') {
            steps {
				echo 'Docker image version'
            }
        }
    }
}