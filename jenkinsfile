pipeline {
    agent {
        docker { 
			image 'jenkins-windows:2.0'
		}
    }
    stages {
        stage('Test') {
            steps {
				bat'jenkins --version'
                bat'java --version'
            }
        }
    }
}