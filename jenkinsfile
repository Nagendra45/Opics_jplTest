pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
               bat 'DATE /T'
			   bat 'TIME /T'
            }
        }
		stage('Mail'){
			echo 'Sending mail to below reciepents'
		}
    }
}